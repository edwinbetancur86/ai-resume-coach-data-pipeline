# ── Core LLM + validation ────────────────────────────────────────────────
openai>=1.40           # OpenAI-compatible client (points at OpenRouter base URL)
instructor>=1.5        # structure-safe LLM outputs on top of Pydantic
pydantic>=2.7          # schema validation with rich error objects
python-dotenv>=1.0     # load .env config
tenacity>=8.3          # retry/backoff around flaky API calls

# ── Data + analysis ──────────────────────────────────────────────────────
pandas>=2.2
numpy>=1.26

# ── Visualization ────────────────────────────────────────────────────────
matplotlib>=3.8
seaborn>=0.13

# ── REST API ─────────────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.30   # ASGI server to run the FastAPI app
httpx>=0.27               # test client / async HTTP

# ── Optional observability (safe to install; only active if keys are set) ─
braintrust>=0.0.170
logfire>=0.50

# ── Dev quality (optional) ───────────────────────────────────────────────
ruff>=0.5
